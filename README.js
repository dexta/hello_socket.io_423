var codeLog = 'Try some Workflow with home <-> github <-> c9.io';
codeLog += 'there are some traps so dancing around ^^';
codeLog += 'Delete some file on home and some different on c9.io';
codeLog += 'looking good so on .... :)';

codeLog += '- 3days later:';
codeLog += 'Add a wisper mode -> type /w username message';
codeLog += 'Fix the Reload Problem -> app.js crash on F5';
codeLog += 'Change some color, and make a darker theme ';
codeLog += ' you are welcome to add some more theming ';

